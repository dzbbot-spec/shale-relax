# Сессия 18 апреля 2026

## Сделано

### Инфраструктура
- Загружены 24 фото в Cloudinary (9 exterior + 14 interior), cloud dfbhw1rfx
- Настроен деплой мини-аппа на GitHub Pages (dzbbot-spec.github.io/shale-relax)
- Репозиторий сделан публичным (был приватным — Pages не работал)
- GitHub Pages включён через API (не через UI)

### Бот и API
- Исправлен Procfile: `worker:` → `web:` — Railway теперь открывает порт
- Добавлен `POST /api/booking` (aiohttp) с CORS для github.io и netlify
- Fallback OWNER_CHAT_ID = 1914219730 в коде
- Обновлён MINI_APP_URL в боте: netlify → dzbbot-spec.github.io/shale-relax

### Мини-апп
- Навигация переделана в стиль iOS (белая панель + blur, без таблетки)
- Слайдер: убрана автопрокрутка, добавлен свайп пальцем (порог 50px)
- Форма бронирования: все поля вертикально, 44px высота, 16px font-size
- iOS-фиксы: maximum-scale=1, font-size 16px, type="date" напрямую
- Кнопки ± с press-эффектом (150ms)
- Cloudinary URL: добавлен q_auto,f_auto,w_800
- Страница «О домиках»: убраны фото, только текстовые блоки
- NavBar: «Забронировать» → «Заявка» (не влезало)

### Исправленные баги
- TypeScript ошибка `const today` не используется — убрана, билд починен
- Неверный Railway URL (shale-relax-production → worker-production-8fd9)
- CORS заголовки на все ответы включая ошибки

### Документация
- Создан TZ_v3.0.md (полное ТЗ с нуля)
- Обновлён PROGRESS.md с таблицей решённых проблем
- Обновлён utils/obsidian_sync.py
- Создан CLAUDE.md (этот файл — контекст для будущих сессий)

## Статус на конец сессии

- Бот работает, мини-апп задеплоен
- 2 заявки в bookings.json
- OpenAI и Kling без баланса — конвейер не тестировался
- Railway триал до ~11 мая 2026

## Следующая сессия

1. Протестировать форму бронирования на iPhone в Telegram
2. Пополнить OpenAI ($5) и запустить тест конвейера
3. Добавить MANAGER_CHAT_ID в Railway env
